scalar UUID
scalar Datatime
scalar Time
scalar Date

enum DirectionOrderBy {
  ASC
  DESC
}

enum SystemConnect {
  Web
  App
}

type AuthPayLoad {
  user: User!
  token: String!
}

type InfoPage {
  count: Int!
  pages: Int!
  current: Int!
  next: Boolean!
  prev: Boolean!
}

type TotalElectores {
  electores: Int!
  a_favor: Int!
  en_contra: Int!
  en_cola: Int!
  en_duda: Int!
}

type InfoMesas {
  aperturadas: Int!
  cerradas: Int!
  sin_aperturar: Int!
}

input OptionsFilterBasic {
  search: String
}

input OptionsSearch {
  orderBy: [String!]
  direction: [DirectionOrderBy!]
  limit: Int
  offset: Int
  currentPage: Int
}
